# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build monorepo-standalone-node-server`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t monorepo-standalone-node-server`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system monorepo-standalone-node-server && \
          adduser --system -G monorepo-standalone-node-server monorepo-standalone-node-server

COPY dist/monorepo-standalone-node-server monorepo-standalone-node-server
RUN chown -R monorepo-standalone-node-server:monorepo-standalone-node-server .

CMD [ "node", "monorepo-standalone-node-server" ]
